import React from 'react'
import ReactDOM from 'react-dom/client'
import LarpMedia from './LarpMedia'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LarpMedia />
  </React.StrictMode>,
)
